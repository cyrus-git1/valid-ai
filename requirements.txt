# Core API (Data Plane)
fastapi
uvicorn[standard]
python-dotenv
pydantic

# Database
supabase
numpy
scipy

# Search (KGRetrieverService)
openai
langchain-core
langchain-openai

# HTTP client
requests
httpx

# Structured logging + observability
structlog

# Auth, rate-limiting, caching (shared Redis)
redis

# Async job queue (Phase 2: async ingest)
arq

# Tests (Phase 3)
pytest
pytest-asyncio
pytest-cov
